import brand from "../../brand.config.json";

export const BRAND = brand;
export const BRAND_NAME = brand.name;
export const BRAND_COLORS = brand.colors;
export const BRAND_FONTS = brand.typography;
export default brand;
